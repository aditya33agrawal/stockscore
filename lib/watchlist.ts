/**
 * Shared watchlist constants.
 *
 * Single source of truth for the watchlist size cap so the API and UI agree.
 */

/** Maximum number of stocks a user may keep on their watchlist. */
export const WATCHLIST_MAX = 50;
